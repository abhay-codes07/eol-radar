schema_version = 1

# Tools the Play's steps actually invoke. Nothing else is declared: a phantom
# dependency is a barrier to somebody running this for the first time.

[[tools]]
id = "python3"
command = "python3"
required = true
version_requirement = ">=3.8"

[[tools]]
id = "git"
command = "git"
required = true
